name: Dev Build (Rolling)

on:
  push:
    branches:
      - dev

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint:all

      - name: Stamp beta version
        shell: bash
        run: |
          BASE=$(node -p "require('./package.json').version")
          BETA_VERSION="${BASE}-beta.${{ github.run_number }}"
          echo "BETA_VERSION=$BETA_VERSION" >> $GITHUB_ENV
          npm version "$BETA_VERSION" --no-git-tag-version

      - name: Build
        run: npm run build:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish dev-latest pre-release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dev-latest
          name: "Beta Build ${{ env.BETA_VERSION }}"
          body: |
            Rolling beta build from `dev` branch.
            Commit: ${{ github.sha }}
            Built: ${{ github.run_number }}

            > Pre-release — for beta testers only.
          files: dist/*.exe
          prerelease: true
          draft: false
